<odoo>
    <data>
        <record id="res_config_settings_view_form" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.inherit.sale</field>
            <field name="model">res.config.settings</field>
            <field name="priority" eval="40" />
            <field name="inherit_id" ref="base.res_config_settings_view_form" />
            <field name="arch" type="xml">
                <xpath expr="//block[@name='catalog_setting_container']" position="after">
                    <block name="website_quote_monthly_uom">
                        <setting>
                            <field name="sale_order_bool" />
                            <div>
                                <label for="sale_order_bool" string="Automatic creation of contracts from sale orders"/>
                            </div>
                            <div class="text-muted">
                                Will activate the automatic creation of contracts from a sale order
                            </div>
                        </setting>
                    </block>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
