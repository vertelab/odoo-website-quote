<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="monthly_quote" name="monthly_quote" inherit_id="sale.sale_order_portal_template">
        <xpath expr="//t[@t-set='title']" position="replace">
            <t t-set="title">
                <t t-set="amount_fixed_total" t-value="'{:_.0f}'.format(round(sale_order.amount_fixed_total)).replace('_',' ')"/>
                <t t-set="monthly_total" t-value="'{:_.0f}'.format(round(sale_order.amount_month_total)).replace('_',' ')"/>
                <t t-set="sale_currency" t-value="sale_order.currency_id.symbol"/>

                <h4 class="mb-0 " t-if="sale_order.amount_fixed_total">
                    Fixed Costs: <b t-field="sale_order.amount_fixed_total_tax" data-id="amount_fixed_total_tax"/>
                </h4>
                <h4 class="mb-0" t-if="sale_order.amount_month_total">
                    Monthly: <b t-field="sale_order.amount_month_total_tax" data-id="amount_month_total_tax" />
                </h4>
            </t>
        </xpath>
    </template>

    <template id="fixed_cost_details" name="fixed costs">
        <section id="details" style="page-break-inside: auto;">
            <t t-set="display_discount" t-value="True in [line.discount > 0 for line in sale_order.order_line]"/>

            <div class="table-responsive">
                <table t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="table table-sm" id="sales_order_table">
                    <thead>
                        <tr>
                            <th class="text-start" id="product_img"/>
                            <th class="text-start" id="product_name_header">Products</th>
                            <th class="text-end" id="product_qty_header">Quantity</th>
                            <th t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                Unit Price
                            </th>
                            <th t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                <span>Disc.%</span>
                            </th>
                            <th t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes_header">
                                <span>Taxes</span>
                            </th>
                            <th class="text-end" id="subtotal_header">
                                <span>Amount</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">
                        <t t-set="current_subtotal" t-value="0"/>

                        <t t-foreach="fixed_payment_lines" t-as="line">
                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>
                            <tr
                                t-att-class="'fw-bold o_line_section' if (
                                    line.display_type == 'line_section'
                                    or line.product_type == 'combo'
                                )
                                else 'fst-italic o_line_note' if line.display_type == 'line_note'
                                else ''"
                            >
                                <t
                                    name="product_line_row"
                                    t-if="not line.display_type and line.product_type != 'combo'"
                                >
                                    <td id="product_img">
                                        <img class="img img-responsive center-block" t-att-src="'/website/image/product.product/%s/image_128/50x50' %line.product_id.id" style="max-height: 50px;" />
                                    </td>
                                    <td id="product_name">
                                        <span t-field="line.name"/>
                                    </td>
                                    <td class="text-end" id="quote_qty_td">
                                        <div id="quote_qty">
                                            <span t-field="line.product_uom_qty"/>
                                            <span t-field="line.product_uom"/>
                                        </div>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-if="line.discount &gt;= 0"
                                            t-field="line.price_unit"
                                            t-att-style="line.discount and 'text-decoration: line-through' or None"
                                            t-att-class="(line.discount and 'text-danger' or '') + ' text-end'"
                                        />
                                        <div t-if="line.discount">
                                            <t t-out="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                                        </div>
                                    </td>
                                    <td t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <strong t-if="line.discount &gt; 0" class="text-info">
                                            <t t-out="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                                        </strong>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes">
                                        <span t-out="', '.join(map(lambda x: (x.name), line.tax_id))"/>
                                    </td>
                                    <td t-if="not line.is_downpayment" class="text-end" id="subtotal">
                                    <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal"/>
                                    </td>
                                </t>
                                <t t-if="line.display_type == 'line_section' or line.product_type == 'combo'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-if="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                </t>
                            </tr>
                            <tr
                                t-if="current_section and (
                                    line_last
                                    or fixed_payment_lines[line_index+1].display_type == 'line_section'
                                    or fixed_payment_lines[line_index+1].product_type == 'combo'
                                    or (
                                        line.combo_item_id
                                        and not fixed_payment_lines[line_index+1].combo_item_id
                                    )
                                ) and not line.is_downpayment"
                                class="is-subtotal text-end"
                            >
                                <t t-set="current_section" t-value="None"/>
                                <td colspan="99">
                                    <strong class="mr16">Subtotal</strong>
                                    <span t-out="current_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": sale_order.currency_id}'
                                    />
                                </td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </div>

            <div id="total" name="total" style="page-break-inside: avoid;">
                <div class="col-xs-7 col-md-5 ms-auto">
                    <table class="table table-sm" name="sale_order_totals_table">
                        <t t-call="sale.document_tax_totals">
                            <t t-set="tax_totals" t-value="sale_order.fixed_tax_totals"/>
                            <t t-set="currency" t-value="sale_order.currency_id"/>
                        </t>
                    </table>
                </div>
            </div>
        </section>
    </template>

    <template id="monthly_cost_details" name="monthly costs">
        <section id="details" style="page-break-inside: auto;">
            <t t-set="display_discount" t-value="True in [line.discount > 0 for line in sale_order.order_line]"/>

            <div class="table-responsive">
                <table t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="table table-sm" id="sales_order_table">
                    <thead>
                        <tr>
                            <th class="text-start" id="product_img"/>
                            <th class="text-start" id="product_name_header">Products</th>
                            <th class="text-end" id="product_qty_header">Quantity</th>
                            <th t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                Unit Price
                            </th>
                            <th t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                <span>Disc.%</span>
                            </th>
                            <th t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes_header">
                                <span>Taxes</span>
                            </th>
                            <th class="text-end" id="subtotal_header">
                                <span>Amount</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">
                        <t t-set="current_subtotal" t-value="0"/>

                        <t t-foreach="monthly_payment_lines" t-as="line">
                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>
                            <tr
                                t-att-class="'fw-bold o_line_section' if (
                                    line.display_type == 'line_section'
                                    or line.product_type == 'combo'
                                )
                                else 'fst-italic o_line_note' if line.display_type == 'line_note'
                                else ''"
                            >
                                <t
                                    name="product_line_row"
                                    t-if="not line.display_type and line.product_type != 'combo'"
                                >
                                    <td id="product_img">
                                        <img class="img img-responsive center-block" t-att-src="'/website/image/product.product/%s/image_128/50x50' %line.product_id.id" style="max-height: 50px;" />
                                    </td>

                                    <td id="product_name">
                                        <span t-field="line.name"/>
                                    </td>
                                    <td class="text-end" id="quote_qty_td">
                                        <div id="quote_qty">
                                            <span t-field="line.product_uom_qty"/>
                                            <span t-field="line.product_uom"/>
                                        </div>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-if="line.discount &gt;= 0"
                                            t-field="line.price_unit"
                                            t-att-style="line.discount and 'text-decoration: line-through' or None"
                                            t-att-class="(line.discount and 'text-danger' or '') + ' text-end'"
                                        />
                                        <div t-if="line.discount">
                                            <t t-out="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                                        </div>
                                    </td>
                                    <td t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <strong t-if="line.discount &gt; 0" class="text-info">
                                            <t t-out="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                                        </strong>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes">
                                        <span t-out="', '.join(map(lambda x: (x.name), line.tax_id))"/>
                                    </td>
                                    <td t-if="not line.is_downpayment" class="text-end" id="subtotal">
                                    <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal"/>
                                    </td>
                                </t>
                                <t t-if="line.display_type == 'line_section' or line.product_type == 'combo'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-if="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                </t>
                            </tr>
                            <tr
                                t-if="current_section and (
                                    line_last
                                    or monthly_payment_lines[line_index+1].display_type == 'line_section'
                                    or monthly_payment_lines[line_index+1].product_type == 'combo'
                                    or (
                                        line.combo_item_id
                                        and not monthly_payment_lines[line_index+1].combo_item_id
                                    )
                                ) and not line.is_downpayment"
                                class="is-subtotal text-end"
                            >
                                <t t-set="current_section" t-value="None"/>
                                <td colspan="99">
                                    <strong class="mr16">Subtotal</strong>
                                    <span t-out="current_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": sale_order.currency_id}'
                                    />
                                </td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </div>

            <div id="total" name="total" style="page-break-inside: avoid;">
                <div class="col-xs-7 col-md-5 ms-auto">
                    <table class="table table-sm" name="sale_order_totals_table">
                        <t t-call="sale.document_tax_totals">
                            <t t-set="tax_totals" t-value="sale_order.monthly_tax_totals"/>
                            <t t-set="currency" t-value="sale_order.currency_id"/>
                        </t>
                    </table>
                </div>
            </div>
        </section>
    </template>

    <template id="quotations" name="quotes" inherit_id="sale.sale_order_portal_content" priority="15">
        <xpath expr="//section[@id='details']" position="before">
            <t t-set="fixed_payment_lines" t-value="sale_order.order_line_fixed_ids"/>
            <t t-set="monthly_payment_lines" t-value="sale_order.order_line_month_ids"/>

            <t t-if="fixed_payment_lines">
                <h2 id="fixed_costs">Fixed Costs</h2>
                <t t-call="website_quote_monthly.fixed_cost_details"/>
            </t>
            <t t-if="monthly_payment_lines">
                <h2 id="monthly_costs">Monthly Costs</h2>
                <t t-call="website_quote_monthly.monthly_cost_details"/>
            </t>
        </xpath>

        <xpath expr="//section[@id='details']" position="attributes">
            <attribute name="style">display: none !important</attribute>
        </xpath>
    </template>


    <template id="optional_products" name="optional_products" inherit_id="sale_management.sale_order_portal_content_inherit_sale_management">
        <xpath expr="//t[@t-if='sale_order._can_be_edited_on_portal() and any((not option.is_present) for option in sale_order.sale_order_option_ids)']/section[1]" position="replace">
            <h2>Optional Products</h2>
            <t t-set="display_discount" t-value="True in [option.discount for option in sale_order.sale_order_option_ids]" />
            <table class="table table-sm">
                <thead>
                    <tr>
                        <th class="text-left">Product</th>
                        <th t-if="display_discount" class="text-right">
                            <span>Discount</span>
                        </th>
                        <th class="text-right">Price</th>
                        <th class="text-right">UoM</th>
                        <th t-if="sale_order.state in ['draft', 'sent'] and report_type == 'html'" />
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="sale_order.sale_order_option_ids" t-as="option">
                        <t t-if="not option.line_id">
                            <td>
                                <div t-field="option.name" />
                            </td>
                            <td t-if="display_discount">
                                <strong t-if="option.discount" class="text-info text-right">
                                    <t t-esc="((option.discount % 1) and '%s' or '%d') % option.discount" />
                                    % discount
                                </strong>
                            </td>

                            <td>
                                <strong class="text-right">
                                    <div t-esc="option.price_unit*option.quantity" t-options='{"widget": "monetary", "display_currency": sale_order.pricelist_id.currency_id}' t-att-style="option.discount and 'text-decoration: line-through' or None" t-att-class="option.discount and 'text-danger' or None" />
                                    <div t-if="option.discount">
                                        <t t-esc="(1-option.discount / 100.0) * option.price_unit" t-options='{"widget": "monetary", "display_currency": sale_order.pricelist_id.currency_id}' />
                                    </div>
                                </strong>

                            </td>
                            <td>
                                <strong class="text-right">
                                    <div t-field="option.uom_id" />
                                </strong>
                            </td>

                            <td class="text-center" t-if="sale_order.state in ['draft', 'sent'] and report_type == 'html'">
                                <a t-att-data-option-id="option.id" href="#" class="mb8 js_add_optional_products d-print-none" aria-label="Add to cart" title="Add to cart">
                                    <span class="fa fa-shopping-cart"/>
                                </a>
                            </td>
                        </t>
                    </tr>
                </tbody>
            </table>
        </xpath>
    </template>
</odoo>
